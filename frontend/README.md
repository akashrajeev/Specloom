# Frontend

Specloom is a modern engineering workspace, not a chat-first UI.

Primary views: Build, Context, System, Test, Deploy, Run.

The System view will use React Flow and consume the canonical Workflow IR.
